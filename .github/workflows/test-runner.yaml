name: Test Runner
on: workflow_dispatch
jobs:
  # Determine runner with runner's labels which must be matched to every label in specified runner
  test-runner:
    runs-on: 
      - self-hosted
      - docker
      - javascript
      
    steps:
      - run: echo "Test runner"
